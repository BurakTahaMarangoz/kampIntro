using System;

namespace Collections
{
    class Program
    {
        static void Main(string[] args)
        {
            string[] names = new string[] {"Ahmet", "Burak", "Veli", "Osman" };

            foreach (string name in names)
            {
                Console.WriteLine(name);
            }

            //new
        }
    }
}
